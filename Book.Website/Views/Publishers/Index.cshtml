@model System.Collections.Generic.IEnumerable<BookRetail_API.Models.Publisher>

@{
    ViewBag.Title = "Publishers";
}

<h2>Publishers</h2>
<ul>
    @foreach (var pbsher in Model) {
        <li><a href="@Url.Action("Models", "Publishers", new { id = pbsher.Code })">@pbsher.Name</a></li>
    }
</ul>