@model System.Collections.Generic.IEnumerable<BookRetail_API.Models.Publisher>

@{
    ViewBag.Title = "Publishers";
}

<h2>Publishers</h2>
<ul>
    @foreach (var mfr in Model) {
        <li><a href="@Url.Action("Models", "Publishers", new { id = mfr.Code })">@mfr.Name</a></li>
    }
</ul>